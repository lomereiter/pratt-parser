#ifndef PARSER_FORWARD_H
#define PARSER_FORWARD_H

template <typename T> class PrattParser;
template <typename T> class Symbol;
template <typename T> class SymbolDict;
template <typename T> class Token;

#endif
